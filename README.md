# Projet database en C

GRIFFON Marvin 3SI3

## fonctionnalités
-insertion de données
-sélection de données
-tests unitaires
-persistance des données

## instructions
- compiler le projet : `make`.
- lancer le programme : `./projet`.
- exécuter les tests : `make test`.
